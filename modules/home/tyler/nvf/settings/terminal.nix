{
  # Terminal settings
  toggleterm = {
    enable = true;
    mappings = {
      open = "<Leader>h";
    };
    setupOpts = {
      direction = "horizontal";
      enable_winbar = true;
    };
  };
}
