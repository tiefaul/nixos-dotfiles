{
  # Terminal settings
  toggleterm = {
    enable = true;
    mappings = {
      open = "<Leader>z";
    };
    setupOpts = {
      direction = "horizontal";
      enable_winbar = true;
    };
  };
}
